#include <stdio.h>
#include<string.h>
int main(void) {
  int n,i;
 char str1[100],str2[100];
  scanf("%s",str1);
  scanf("%s",str2);
  n=strcmpi(str1,str2);
  if(n==0)
    printf("0");
  else if(n<0)
    printf("-1");
  else if(n>0)
    printf("1");
  return 0;
}
