#include <bits/stdc++.h>
using namespace std;
int main(){
	int n,v,x=0;
	cin >> n;
	cin >> v;
	if(n-1>v){
		x=v;
    	for(int i=2;i<=(n-v);i++){
		    x+=i;
	    }
    }
    else if(n-1==v){
    	x=v;
    }
    else{
    	x=(n-1);
    }
	cout<< x;
}
